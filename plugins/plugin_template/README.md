# Plugin Template
Copy this folder to create a new plugin.
**IMPORTANT**: All user-facing strings must be localized in `locales/ru.json` and `locales/en.json` (at minimum) to support the multilingual PBX architecture.
